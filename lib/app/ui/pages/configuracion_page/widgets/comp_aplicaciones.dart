import 'package:flutter/material.dart';

class CompAplicaciones extends StatelessWidget {
  const CompAplicaciones({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}